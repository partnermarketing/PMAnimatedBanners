window.pmAnimatedBannersConfig = function(conf) {
  conf.map({
    reference: 'logo',
    image: 'image.jpg',
    align: {
      x: 'center',
      y: 'center'
    }
  });
  conf.map({
    reference: 'stage',
    link: 'http://partnermarketing.com'
  });
};